import { addN, add2, sub2, mult7, div25, int } from './number'
import { login, logout } from './user'

const action = {
  addN,
  add2,
  sub2,
  mult7,
  div25,
  int,
  login,
  logout
}

export default action